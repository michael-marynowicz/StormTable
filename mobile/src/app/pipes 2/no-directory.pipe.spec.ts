import { NoDirectoryPipe } from './no-directory.pipe';

describe('NoDirectoryPipe', () => {
  it('create an instance', () => {
    const pipe = new NoDirectoryPipe();
    expect(pipe).toBeTruthy();
  });
});
